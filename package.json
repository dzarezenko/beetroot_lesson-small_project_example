{
  "name": "beetroot_lesson-small_project_example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "rm -rf dist/ && npm run build"
  },
  "dependencies": {
    "vue": "^2.6.10",
    "vue-web-storage": "^4.0.0"
  },
  "devDependencies": {
    "@vue/cli-service": "^4.1.0",
    "vue-template-compiler": "^2.6.10"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions"
  ]
}
